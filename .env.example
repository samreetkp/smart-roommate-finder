VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: long-term rental listings in the Apartments tab (https://developers.rentcast.io).
# Use a server-side proxy in production; `npm run dev` and `npm run preview` proxy /api/rentcast automatically.
RENTCAST_API_KEY=
